Calculator app that uses AST-Based Parser instead of the unsafe built-in eval() fucntion, 
and uses tkinter and pillow libary for the GUI.

Operating sys:
  Windows 11

Using:
  python 3.14

libary:
  tkinter 8.6.15
  pillow 12.0.0
  
